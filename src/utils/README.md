# utils/

Domain-specific utility functions.

Unlike `lib/` (low-level, generic utilities), `utils/` contains business-logic helpers.

## Examples

- `formatCurrency(value)` — Format BRL currency
- `parseDate(str)` — Parse date strings
- `generateId()` — UUID generation
- `truncateText(text, maxLen)` — Truncate with ellipsis
