# contexts/

React Context definitions for the Cash AI application.

Each context defines `createContext` + types. Providers live in `../providers/`.

## Planned Contexts

- `AuthContext` — User authentication state
- `ThemeContext` — Theme preferences (light/dark)
- `ChatContext` — Active conversation state
- `SidebarContext` — Sidebar open/collapsed state
